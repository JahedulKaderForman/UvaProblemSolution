#include <stdio.h>
#include <string.h>

int main()
{
	char num[100];
	int dec = 0, i, j, len;
	printf("Enter a number: ");
	gets(num);
	len = strlen(num);
	for(i=1; i<=5; i++){
		printf("%c",a[i]);
	}
	return 0;
}
